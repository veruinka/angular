import { UsedEmailDirective } from './used-email.directive';

describe('UsedEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new UsedEmailDirective();
    expect(directive).toBeTruthy();
  });
});
