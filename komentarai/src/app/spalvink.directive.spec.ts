import { SpalvinkDirective } from './spalvink.directive';

describe('SpalvinkDirective', () => {
  it('should create an instance', () => {
    const directive = new SpalvinkDirective();
    expect(directive).toBeTruthy();
  });
});
